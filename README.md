# ghidra-no-mcp
thoughts from `https://github.com/P4nda0s/IDA-NO-MCP`
# install 
* download ghidra and set env `GHIDRA_INSTALL_DIR` to it
* `pip install pyghidra`
# usage
```
python ghidra-export.py xx.exe [export-dir]
```
